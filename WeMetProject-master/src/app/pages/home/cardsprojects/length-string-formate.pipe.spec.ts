import { LengthStringFormatePipe } from './length-string-formate.pipe';

describe('LengthStringFormatePipe', () => {
  it('create an instance', () => {
    const pipe = new LengthStringFormatePipe();
    expect(pipe).toBeTruthy();
  });
});
