import { LenghtStringFormatPipe } from './lenght-string-format.pipe';

describe('LenghtStringFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new LenghtStringFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
